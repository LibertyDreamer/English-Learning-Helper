import { makeWordsClickable } from './clickableWords.js';

function handleWordClick(clickedWord, context) {
  // Your custom code for handling the clicked word and its context.
  console.log('Clicked word:', clickedWord);
  console.log('Context:', context);
}

// Assuming your story content is in an element with id "story"
//makeWordsClickable('story', handleWordClick);
